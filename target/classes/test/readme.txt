用来采集图书的ISBN编号编码、出版社、出版时间、版次、正文语种、定价等信息。

1. 需要修改test.config包下面的Memory类，可以修改

	-- 是否使用代理IP
	-- 图片保存路径
	-- 代理IP的API接口
	-- 线程池数量
	-- 默认超时时间
	
2. 项目需要用Maven引入，打开后如果有报错，可以检查是否为JDK版本问题

3. 运行Starter类启动爬虫